Requirements for the Node Class:

public Node(int data) - Constructor that takes an int value to be stored as its data
public int getData() - returns the data element of the node
public Node getNext() - returns the next linked node or null otherwise
public void setNext(Node next) - sets the next linked node
